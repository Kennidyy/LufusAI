{
  "name": "lufus-desktop",
  "version": "1.0.0",
  "description": "LufusAI Desktop Application",
  "main": "dist/main/index.js",
  "type": "module",
  "scripts": {
    "build": "node build-views.mjs",
    "dev": "bun run build && electron ."
  },
  "dependencies": {
    "argon2": "^0.44.0",
    "bcryptjs": "^3.0.3",
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "esbuild": "^0.19.0"
  }
}
